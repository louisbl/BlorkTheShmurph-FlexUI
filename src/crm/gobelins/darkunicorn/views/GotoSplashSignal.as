package crm.gobelins.darkunicorn.views
{
	import org.osflash.signals.Signal;
	
	public class GotoSplashSignal extends Signal
	{
		public function GotoSplashSignal(...parameters)
		{
			super(parameters);
		}
	}
}